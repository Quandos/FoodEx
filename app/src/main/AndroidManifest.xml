<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodonilineshop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignInActivity"></activity>
        <activity android:name=".SellerTermsActivity" />
        <activity android:name=".CustomerTermsActivity" />
        <activity android:name=".SellerSignUpActivity" />
        <activity android:name=".CustomerSignUpActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".StoreItems" />
        <activity android:name=".TermsAndCondition">

        </activity>
        <activity android:name=".StoreDetailes" />
        <activity android:name=".PersonalDetailes" />
        <activity android:name=".SingUp" />

        <activity android:name=".MainActivity" />
    </application>

</manifest>